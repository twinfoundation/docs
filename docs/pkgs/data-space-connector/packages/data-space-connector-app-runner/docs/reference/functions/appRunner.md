# Function: appRunner()

> **appRunner**\<`T`\>(`engineCloneData`, `payload`): `Promise`\<`T`\>

Data Space Connector Task.

## Type Parameters

### T

`T`

## Parameters

### engineCloneData

`IEngineCoreClone`

The Engine.

### payload

`IExecutionPayload`

The payload

## Returns

`Promise`\<`T`\>

The execution result.
