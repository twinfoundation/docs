# Function: buildCommandBuild()

> **buildCommandBuild**(`program`): `void`

Build the build command to be consumed by the CLI.

## Parameters

### program

`Command`

The command to build on.

## Returns

`void`
