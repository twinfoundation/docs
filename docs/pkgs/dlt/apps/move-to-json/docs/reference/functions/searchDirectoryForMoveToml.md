# Function: searchDirectoryForMoveToml()

> **searchDirectoryForMoveToml**(`dir`, `moveTomlFiles`): `Promise`\<`void`\>

Recursively search a directory for Move.toml files.

## Parameters

### dir

`string`

Directory to search

### moveTomlFiles

`string`[]

Array to collect found files

## Returns

`Promise`\<`void`\>
